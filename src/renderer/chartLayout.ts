class ChartLayout {
  width: number;

  height: number;

  title: string;

  constructor(width: number, height: number, title: string) {
    this.width = width;
    this.height = height;
    this.title = title;
  }
}

export default ChartLayout;
